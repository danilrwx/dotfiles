macos-option-as-alt = true

keybind = alt+left=unbind
keybind = alt+right=unbind

font-family = Iosevka NF
font-thicken = true
font-thicken-strength = 200

maximize = true

window-padding-x = 8
window-padding-y = 5

font-size = 17
adjust-cell-width = -10%
adjust-cell-height = 10%

theme = base

# Honour OSC 52 clipboard writes from the devbox (vim/tmux) without a prompt.
clipboard-write = allow
# Allow OSC 52 reads too, so the devbox `imgpaste` can pull an image the host
# put on the clipboard. Note: this lets any app read the clipboard without a
# prompt; set to "ask" if you'd rather confirm each read.
clipboard-read = allow

