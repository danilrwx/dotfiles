#!/usr/bin/env bash

log() {
  echo -e "\033[0;31m$1\033[0m"
}

# Some useful functions ─────────────────────────────────────────────────────────────────────────────────────────────────
OS=$(uname -s)
if [ -f /etc/os-release ]; then
  . /etc/os-release

  if [ "$ID" == "ubuntu" ]; then
    log "This is a Ubuntu system."
  elif [ "$ID" == "fedora" ]; then
    log "This is a Fedora system."
  else
    exit
  fi
fi

# SSH remote and private submodules (need an ssh key) ─────────────────────────────────────────────────────────────────
if compgen -G "$HOME/.ssh/id_*" | grep -qv '\.pub$'; then
  log "Switching origin to ssh and updating submodules"
  git remote set-url origin git@github.com:danilrwx/dotfiles.git

  if ! git submodule update --init --recursive; then
    log "Failed to update submodules. Continue installation."
  fi
else
  log "No ssh key in ~/.ssh. Skip ssh remote and private submodules."
fi

# Create directories and synlinks to config files ───────────────────────────────────────────────────────────────────────
log "Linking configs"
mkdir -p $HOME/.config
mkdir -p $HOME/.local/share

# CLI/shell configs shared with the devbox container
bash $HOME/dotfiles/devcontainer/links.sh

# Host-only GUI terminal configs
ln -snf $HOME/dotfiles/config/kitty $HOME/.config/
ln -snf $HOME/dotfiles/config/alacritty $HOME/.config/
ln -snf $HOME/dotfiles/config/ghostty $HOME/.config/

# Treesitter parsers (.so, per-machine) + queries, without the nvim-treesitter
# plugin. Queries are vendored in the repo; parsers are compiled here.
if command -v cc >/dev/null 2>&1; then
  log "Building treesitter parsers"
  nvim --headless -c 'TSBuild' -c 'qa' || log "TSBuild failed. Continue installation."
fi

# Private CLI configs (claude, pi, CLAUDE.md) are symlinked by private/install,
# which also keeps those paths out of this public repo.
if [ -f $HOME/dotfiles/private/install ]; then
  log "Running private install"
  if ! "$HOME/dotfiles/private/install"; then
    log "Failed to run private install. Continue installation."
  fi
fi

# Install packages for different OSes ───────────────────────────────────────────────────────────────────────────────────
if [ "$ID" == "fedora" ]; then
  log "Installing fedora packages"
  sudo dnf install https://download1.rpmfusion.org/free/fedora/rpmfusion-free-release-$(rpm -E %fedora).noarch.rpm
  sudo dnf install https://download1.rpmfusion.org/nonfree/fedora/rpmfusion-nonfree-release-$(rpm -E %fedora).noarch.rpm

  sudo dnf update -y

  sudo dnf group install c-development

  sudo dnf install -y lame\* --exclude=lame-devel
  sudo dnf install -y gstreamer1-plugins-{bad-\*,good-\*,base} gstreamer1-plugin-openh264 gstreamer1-libav --exclude=gstreamer1-plugins-bad-free-devel
  # dev tools (tmux, gpg, …) come from brew via tools/repo.sh; keep only the
  # system/GUI packages and brew prerequisites (git, make) here.
  sudo dnf install -y git make wget zip unzip curl file procps-ng tigervnc openssh-askpass pinentry-gnome3 virt-manager \
	xclip xsel zsh telegram-desktop discord
elif [ "$ID" == "ubuntu" ]; then
  log "Installing ubuntu packages"

  sudo apt update -y

  # dev tools (tmux, gpg, …) come from brew via tools/repo.sh; keep only the
  # system/GUI packages and brew prerequisites (build-essential, git) here.
  sudo apt install -y build-essential git wget zip unzip curl file procps tigervnc-viewer ssh-askpass pinentry-gnome3 \
	virt-manager xclip xsel zsh
fi


# Install Homebrew ──────────────────────────────────────────────────────────────────────────────────────────────────────
if ! command -v brew &> /dev/null; then
  log "Installing homebrew"
  NONINTERACTIVE=1 /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
fi
for b in /opt/homebrew/bin/brew /home/linuxbrew/.linuxbrew/bin/brew; do
  [ -x "$b" ] && eval "$($b shellenv)"
done


# Install DevPod CLI (runs the devbox dev container: docker locally, kubernetes
# for remote; forwards ssh-agent and gpg-agent) ─────────────────────────────────────────────────────────────────────────
if ! command -v devpod &> /dev/null; then
  log "Installing devpod CLI"
  arch=$(uname -m | sed 's/x86_64/amd64/;s/aarch64/arm64/')
  os=$(uname -s | tr '[:upper:]' '[:lower:]')
  mkdir -p "$HOME/.local/bin"
  curl -fsSL -o "$HOME/.local/bin/devpod" \
    "https://github.com/loft-sh/devpod/releases/latest/download/devpod-${os}-${arch}"
  chmod +x "$HOME/.local/bin/devpod"
  bash "$HOME/dotfiles/bin/devbox" provider || true
fi


# Host tooling: launcher + ops CLIs + the Go dev toolchain ──────────────────────────────────────────────────────────────
log "Installing host tools"
brew install go kubectl gh glab tmux fzf jq yq gnupg watch htop lazygit ugrep node neovim
[[ "$OS" == "Darwin" ]] && brew install pinentry-mac

# Go dev tools into ~/go/bin (same set as the devbox image, devcontainer/tools/go.sh)
log "Installing go tools"
go install github.com/onsi/ginkgo/v2/ginkgo@v2.14.0
go install github.com/golangci/golangci-lint/v2/cmd/golangci-lint@latest
go install golang.org/x/tools/gopls@latest
go install mvdan.cc/gofumpt@latest
go install golang.org/x/tools/cmd/goimports@latest
go install github.com/go-delve/delve/cmd/dlv@latest
go install github.com/matryer/moq@latest
go install github.com/google/go-containerregistry/cmd/crane@latest
go install github.com/go-task/task/v3/cmd/task@latest

# claude-code via the native installer (self-updating, no node needed)
log "Installing claude-code"
curl -fsSL https://claude.ai/install.sh | bash

# d8 / deckhouse-cli (official installer; default INSTALL_DIR is /opt not on PATH)
if ! command -v d8 &> /dev/null; then
  log "Installing d8"
  INSTALL_DIR=/usr/local/bin UNATTENDED=yes sh -c "$(curl -fsSL https://raw.githubusercontent.com/deckhouse/deckhouse-cli/main/tools/install.sh)"
fi


# Use zsh as the login shell (macOS already defaults to it; install + chsh on
# Linux) ────────────────────────────────────────────────────────────────────────────────────────────────────────────────
if [[ "$OS" == "Linux" ]]; then
  zsh_bin="$(command -v zsh || true)"
  if [ -n "$zsh_bin" ] && [ "$SHELL" != "$zsh_bin" ]; then
    grep -qw "$zsh_bin" /etc/shells || echo "$zsh_bin" | sudo tee -a /etc/shells
    log "Setting zsh as default shell"
    chsh -s "$zsh_bin"
  fi
fi


# Shell extras (completions + tmux plugins) shared with the devbox container ────────────────────────────────────────────
log "Installing shell extras"
bash "$HOME/dotfiles/devcontainer/shell-setup.sh"


# Linux desktop: docker, groups, fonts ─────────────────────────────────────────────────────────────────────────────────
if [[ "$OS" == "Linux" ]]; then
  # install docker ──────────────────────────────────────────────────────────────────────────────────────────────────────
  if [ ! -f /usr/bin/docker ]; then
    log "Installing docker"
    curl -fsSL get.docker.com | bash

    log "Starting docker"
    sudo systemctl start docker.socket
    log "Enabling docker"
    sudo systemctl enable docker.socket
  fi

  # add user to docker group ────────────────────────────────────────────────────────────────────────────────────────────
  if ! groups | grep -q '\bdocker\b'; then
    log "Adding user to docker group"
    sudo usermod -aG docker $USER
  fi

  # add user to libvirt group ───────────────────────────────────────────────────────────────────────────────────────────
  if ! groups | grep -q '\blibvirt\b'; then
    log "Adding user to libvirt group"
    sudo usermod -aG libvirt $USER
  fi

  # download Iosevka nerd font ──────────────────────────────────────────────────────────────────────────────────────────
  if [ ! -f $HOME/.local/share/fonts/IosevkaNerdFont-Medium.ttf ]; then
    log "Downloading Iosevka nerd font"
    wget https://github.com/ryanoasis/nerd-fonts/releases/download/v3.3.0/Iosevka.zip
    mkdir -p $HOME/.local/share/fonts
    cd $HOME/.local/share/fonts && unzip $HOME/dotfiles/Iosevka.zip
    rm $HOME/dotfiles/Iosevka.zip
    fc-cache -f
  fi

  # download Apple Emoji font ───────────────────────────────────────────────────────────────────────────────────────────
  if [ ! -f $HOME/.local/share/fonts/AppleColorEmoji.ttf ]; then
    log "Downloading Apple Emoji font"
    wget -P $HOME/.local/share/fonts https://github.com/samuelngs/apple-emoji-linux/releases/download/v18.4/AppleColorEmoji.ttf
    fc-cache -f
  fi
fi
